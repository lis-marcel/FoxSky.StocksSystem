services:
  rabbitmq:
    build: ./RabbitMQ
    # Or use the image directly without Dockerfile:
    # image: rabbitmq:4-management
    container_name: rabbitmq
    ports:
      - "5672:5672"   # AMQP port
      - "15672:15672" # Management UI port
    environment:
      RABBITMQ_DEFAULT_USER: admin
      RABBITMQ_DEFAULT_PASS: admin
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq
    networks:
      - rabbitmq_network
    restart: unless-stopped

volumes:
  rabbitmq_data:

networks:
  rabbitmq_network:
    driver: bridge
